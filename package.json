{
  "name": "tslint-object-literal-smart-keys",
  "version": "0.3.1",
  "description": "Rule for tslint",
  "main": "./lib/objectLiteralSmartKeys.js",
  "scripts": {
    "check": "tslint --test ./tests",
    "dev": "tsc --watch --sourceMap --project ./src",
    "lint": "tslint --project ./src/tsconfig.json --type-check",
    "prepublish": "rm -rf rules && tsc --project src",
    "test": "npm run lint && npm run check"
  },
  "keywords": [
    "tslint",
    "rule"
  ],
  "files": [
    "rules"
  ],
  "repository": {
    "type": "git",
    "url": "git@github.com:arusakov/tslint-object-literal-smart-keys.git"
  },
  "author": "Alexander Rusakov <a_s_rusakov@mail.ru> (https://github.com/arusakov)",
  "license": "MIT",
  "peerDependencies": {
    "tslint": "^3.15.1",
    "typescript": "^2.0.3"
  },
  "devDependencies": {
    "tslint": "3.15.1",
    "typescript": "2.0.3"
  },
  "engines": {
    "node": "^6.0.0"
  }
}
